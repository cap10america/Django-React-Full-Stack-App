implementation:
  type: service
  container:
    image: your-image-name
  ports:
    - name: 'REST -API'
      port: 8000
      protocol: REST # Or REST, if Choreo uses that
  expose:
    - context: /
      visibility: Public
